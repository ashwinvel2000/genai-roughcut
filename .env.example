# Script-to-Rough-Cut Environment Variables

# Server Configuration
PORT=8080

# Script Generation Mode
# Options: "template" | "transformers" | "openai"
# - template: Deterministic template-based scripts (always free)
# - transformers: DistilGPT-2 model via HuggingFace (free, requires ~500MB download on first run)
# - openai: OpenAI GPT-4o-mini (requires OPENAI_API_KEY and credits)
SCRIPT_MODE=transformers

# Text-to-Speech Mode
# Options: "gtts" | "elevenlabs"
# - gtts: Google Text-to-Speech (free, requires internet)
# - elevenlabs: ElevenLabs API (requires ELEVENLABS_API_KEY and credits)
TTS_MODE=gtts

# Image Generation Mode
# Options: "pollinations" | "pexels" | "pillow"
# - pollinations: AI-generated images via Pollinations.ai (free, unlimited, no API key)
# - pexels: Stock photos via Pexels API (requires PEXELS_API_KEY, free tier available)
# - pillow: Simple colored placeholders (always works)
IMAGE_MODE=pollinations

# API Keys (all optional - only needed for specific modes)
# OpenAI (only needed if SCRIPT_MODE=openai)
OPENAI_API_KEY=

# ElevenLabs (only needed if TTS_MODE=elevenlabs)
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM

# Pexels (only needed if IMAGE_MODE=pexels)
PEXELS_API_KEY=
